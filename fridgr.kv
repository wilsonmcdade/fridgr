# fridgr.kv
# This Kivy file describes the layout
# @author Wilson McDade, wmcda.de

<HomeScreen>
        name: "HomeScreen"

        canvas:
                Color:
                        rgba: 1, 1, 1, 1
                Rectangle:
                        pos: self.pos
                        size: self.size 

        Label:
                text:"HomeScreen"

<NewLabel>
        name: "NewLabel"

        canvas:
                Color:
                        rgba: .8, .8, .8, .8
                Rectangle:
                        pos: self.pos
                        size: self.size 


        Label:
                text:"NewLabel"

<AutoScan>
        name: "AutoScan"

        canvas:
                Color:
                        rgba: .1, .1, .1, .1
                Rectangle:
                        pos: self.pos
                        size: self.size 

        Label:
                text:"AutoScan"

<ItemLookup>
        name: "ItemLookup"

        canvas:
                Color:
                        rgba: .5, .5, .5, .5
                Rectangle:
                        pos: self.pos
                        size: self.size 


        Label:
                text:"ItemLookup"



